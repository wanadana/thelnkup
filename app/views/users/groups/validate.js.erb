<% if @group.title == "" %>
  var errorMsg = $("#validation-fail")
  errorMsg.html("Please enter a valid link").show();
  $('.form-group').addClass('validation-fail');

  setTimeout(function(){
    errorMsg.fadeOut()
    $('.form-group').removeClass('validation-fail');
  }, 4000);
<% else %>
$('#group_form').html("<%= j render partial: 'form' %>");
<% end %>
